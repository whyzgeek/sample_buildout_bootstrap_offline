sample_buildout_bootstrap_offline
=================================

A working sample buildout and bootstrap that installs everything offline

# Usage

```
buildout

```

Then follow instructions in buildout.cfg

